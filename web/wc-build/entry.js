// The whole of the page's third-party signing surface, in one line: everything the airdrop page imports from
// the connector comes through here, so what the page can load is exactly what this file exports.
export { EthereumProvider } from '@walletconnect/ethereum-provider';
